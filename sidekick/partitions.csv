# Sidekick partition table — AtomS3 Lite (ESP32-S3FN8, 8 MB).
#
# Two OTA slots, no factory. Firmware updates are phone-relayed into an OTA
# slot (RFC 0004 §4); the ROM/USB-C path remains first install and hard
# recovery. With otadata erased the bootloader boots ota_0, which is what the
# ADV's USB-C bootstrap writes.
#
# 3 MB slots on an 8 MB part. Measured 2026-09-09 on esp32s3: IDF's
# simple_ota_example (WiFi + HTTPS + CA) is 911 KB; a sidekick without a TLS
# stack is smaller, and 3 MB is roughly 2x headroom — resizing later costs a
# USB-C reflash of every device. Cheap at one device, expensive after
# distribution.
#
# web (LittleFS) claims the free tail above ota_1 (~1.9 MB). It is the sole
# HTTP origin for HTML/JS/CSS once hydrated (RFC 0004 §3). Firmware still
# embeds sidekick/main/web/* as the hydrate seed; the FS is what is served.
# nvs stays at 0x9000 with its original 24 KB — move it and WiFi/token config
# are silently lost.
#
# Name,     Type, SubType,  Offset,   Size,    Flags
nvs,        data, nvs,      0x9000,   0x6000,
otadata,    data, ota,      0xF000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x300000,
ota_1,      app,  ota_1,    0x320000, 0x300000,
web,        data, littlefs, 0x620000, 0x1E0000,
